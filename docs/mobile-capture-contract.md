# Akasha Mobile Capture — Integration Contract

> **Authority:** This document lives in `akasha-pkm-ref`. The PKM repo defines the schema surface; the mobile app consumes it.
> **TID:** [akasha_mobile_capture_tid.md](./akasha_mobile_capture_tid.md)
> **Last updated:** 2026-06-30

## 1. Schema (PKM → App)

The PKM repo publishes a schema file to R2 so the app knows which domains and MOCs are valid. This file is generated by `bin/akasha-sync-schema.sh` and uploaded to the R2 bucket.

### 1.1 `akasha-schema.json` — shape

```json
{
  "version": 1,
  "generated": "2026-06-30T21:00:00Z",
  "domains": [
    {
      "id": "math",
      "label": "Mathematics",
      "mocs": [
        { "id": "linear-algebra", "label": "Linear Algebra" },
        { "id": "calculus", "label": "Calculus" }
      ]
    },
    {
      "id": "cs",
      "label": "Computer Science",
      "mocs": []
    }
  ]
}
```

**Rules:**
- `domain.id` matches the folder name under `Knowledge/` (lowercase, kebab-case where multi-word)
- `mocs[].id` matches the MOC name listed in that domain's `_moc-registry.md` (derived from the MOC note's `title` field, lowercased and kebab-cased)
- Domains with no active MOCs still appear, with an empty `mocs` array
- `version` is a monotonic integer; bump on any breaking schema change

### 1.2 App behavior

- Fetch `akasha-schema.json` on app launch and cache it locally
- Re-fetch on background fetch or manual refresh
- Use `domain.id` and `moc.id` values verbatim in the capture payload (section 2)
- If the schema fetch fails, fall back to the last cached version

## 2. Capture Payload (App → R2)

The app uploads one JSON manifest plus the scanned images to the R2 bucket.

### 2.1 JSON manifest — shape

```json
{
  "session_id": "math_20260630T210500Z",
  "timestamp": "2026-06-30T21:05:00Z",
  "images": [
    "math_20260630T210500Z_page1.jpg",
    "math_20260630T210500Z_page2.jpg",
    "math_20260630T210500Z_page3.jpg"
  ],
  "domain": "math",
  "mocs": ["linear-algebra", "calculus"]
}
```

**Field rules:**

| Field | Required | Type | Notes |
|-------|----------|------|-------|
| `session_id` | yes | string | Unique per capture session. Format: `{domain}_{ISO8601_compact}` |
| `timestamp` | yes | ISO 8601 string | Capture time in UTC |
| `images` | yes | string[] | At least 1 entry. Filenames must match the actual uploaded image files |
| `domain` | yes | string | Must be a `domain.id` from the schema |
| `mocs` | yes | string[] | Array of `moc.id` values from the schema. May be empty if the domain has no MOCs |

### 2.2 Image format

- Format: JPEG
- Max size: ~200 KB per image
- Resolution: auto-optimized by `react-native-document-scanner-plugin` (perspective-corrected, adaptive B&W threshold)
- Naming: `{session_id}_page{N}.jpg` (1-indexed)

### 2.3 R2 object layout

```
<BUCKET>/
  manifests/
    {session_id}.json       # the JSON manifest
  images/
    {session_id}_page1.jpg  # each scan image
    {session_id}_page2.jpg
    ...
  schema/
    akasha-schema.json       # published by the PKM repo
```

The app writes to `manifests/` and `images/`. It reads from `schema/`.

## 3. Environment Variables

These must be set in the environment where the PKM daemon scripts run (laptop/desktop). The mobile app uses its own env (`EXPO_PUSH_TOKEN` + R2 endpoint and credentials).

| Variable | Set By | Used By | Description |
|----------|--------|---------|-------------|
| `AKASHA_R2_ENDPOINT` | User (`.env`) | `akasha-sync-schema.sh`, `akasha-pull.sh` | Cloudflare R2 S3-compatible endpoint URL |
| `AKASHA_R2_BUCKET` | User (`.env`) | `akasha-sync-schema.sh`, `akasha-pull.sh` | R2 bucket name |
| `AKASHA_R2_ACCESS_KEY` | User (`.env`) | `akasha-sync-schema.sh`, `akasha-pull.sh` | R2 access key ID |
| `AKASHA_R2_SECRET_KEY` | User (`.env`) | `akasha-sync-schema.sh`, `akasha-pull.sh` | R2 secret access key |
| `EXPO_PUSH_TOKEN` | User (`.env`) | `akasha-notify.sh` | Expo push token for the mobile device |

The mobile app uses its own Expo-managed environment for `EXPO_PUSH_TOKEN` and R2 credentials — these are separate from the PKM `.env` file.

## 4. PKM Ingestion Pipeline

### 4.1 Pull (`bin/akasha-pull.sh`)

Runs periodically (cron, systemd timer, or manual trigger).

1. List `manifests/` in the R2 bucket
2. For each new manifest:
   - Download `manifests/{session_id}.json` and all referenced images
   - Write them to a temporary directory: `StudyMaterials/inbox/{session_id}/`
   - Delete the manifest and images from R2 (zero cloud state)
3. For each downloaded session, invoke `akasha-material-parser` with the session directory path

### 4.2 Parse (`.commandcode/agents/akasha-material-parser.md`)

Updated to handle image-based capture sessions (not just PDFs).

1. Read `{session_dir}/manifest.json`
2. Read all images in the session directory
3. Pass images to the local Vision LLM for LaTeX transcription
4. Create a math note at `Knowledge/{domain}/{title-slug}.md` using `Templates/math.md` with:
   - `domain:` set to the payload's `domain`
   - `tags:` populated from the payload's `mocs`
   - `image_source:` set to the session directory path
   - `created:` set to the payload's `timestamp`
5. On success: move raw images to `_assets/{session_id}/` and commit
6. **On failure** (LLM output fails syntax linting or returns low-confidence marker):
   - Execute `bin/akasha-notify.sh {session_id}`
   - Leave the raw images in `StudyMaterials/inbox/{session_id}/` for manual inspection

### 4.3 Notify (`bin/akasha-notify.sh`)

Sends an Expo push notification to the device.

```json
{
  "to": "<EXPO_PUSH_TOKEN from env>",
  "title": "Akasha Alert: Parse Failed",
  "body": "Session '<session_id>' could not be read. Please rescan.",
  "data": {
    "session_id": "<session_id>",
    "type": "parse_failure"
  }
}
```

The app must handle the `"parse_failure"` data type by surfacing an in-app alert or badge on the failed session.

## 5. Contract Versioning

This contract uses semantic versioning. The version in `akasha-schema.json` tracks the schema shape only. This document's version tracks the full contract (schema + payload + pipeline).

| Contract Version | Schema Version | Changes |
|-----------------|----------------|---------|
| 1.0.0 | 1 | Initial release |
| 1.0.1 | 1 | PKM-side implementation complete: sync/pull/notify scripts + parser agent update |

**Compatibility rule:** The mobile app must refuse to capture if `akasha-schema.json.version` is higher than the version it was built against, and prompt the user to update the app.

## 6. Implementation Checklist (PKM Side)

- [x] `bin/akasha-sync-schema.sh` — schema generation + R2 upload
- [x] `bin/akasha-pull.sh` — R2 polling + session download + parser trigger
- [x] `bin/akasha-notify.sh` — Expo push notification on parse failure
- [x] `.commandcode/agents/akasha-material-parser.md` — image-session codepath
- [x] `bin/akasha-nightly.sh` — schema sync hooked in as step [1/5]
- [x] `.gitignore` — `.akasha/akasha-schema.json` ignored
- [ ] R2 bucket created and credentials set in `.env`
- [ ] `EXPO_PUSH_TOKEN` set in `.env`
- [ ] `akasha-pull.sh` scheduled (cron/systemd timer)
